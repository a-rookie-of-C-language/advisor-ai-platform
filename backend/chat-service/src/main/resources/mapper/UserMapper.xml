<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.edu.cqut.advisorplatform.mapper.UserMapper">

    <resultMap id="userResultMap" type="cn.edu.cqut.advisorplatform.entity.UserDO">
        <id column="id" property="id" javaType="Long"/>
        <result column="username" property="username" javaType="String"/>
        <result column="email" property="email" javaType="String"/>
        <result column="role" property="role" javaType="String"/>
        <result column="is_deleted" property="isDeleted" javaType="Boolean"/>
        <result column="created_at" property="createdAt" javaType="java.time.LocalDateTime"/>
        <result column="updated_at" property="updatedAt" javaType="java.time.LocalDateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.edu.cqut.advisorplatform.entity.UserDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_user (username, email, role, is_deleted, created_at, updated_at)
        VALUES (#{username}, #{email}, #{role}, #{isDeleted,jdbcType=BIT}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="cn.edu.cqut.advisorplatform.entity.UserDO">
        UPDATE chat_user
        SET username = #{username},
            email = #{email},
            role = #{role},
            is_deleted = #{isDeleted,jdbcType=BIT},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="Long">
        DELETE FROM chat_user WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="Long" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="String" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE username = #{username} AND is_deleted = false
    </select>

    <select id="selectByUsernameContaining" parameterType="String" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE username LIKE CONCAT('%', #{username}, '%') AND is_deleted = false
    </select>

    <select id="selectByRole" parameterType="String" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE role = #{role} AND is_deleted = false
        ORDER BY created_at DESC
    </select>

    <select id="selectAll" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE is_deleted = false
        ORDER BY created_at DESC
    </select>

    <select id="selectPage" resultMap="userResultMap">
        SELECT id, username, email, role, is_deleted, created_at, updated_at
        FROM chat_user
        WHERE is_deleted = false
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM chat_user
        WHERE is_deleted = false
    </select>

    <select id="existsByUsername" parameterType="String" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM chat_user
        WHERE username = #{username} AND is_deleted = false
    </select>

    <update id="markDeleted" parameterType="Long">
        UPDATE chat_user SET is_deleted = true, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>
