<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.edu.cqut.advisorplatform.checkin.attendance.mapper.CourseScheduleMapper">
    <insert id="insert" parameterType="cn.edu.cqut.advisorplatform.checkin.attendance.entity.CourseSchedule"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO course_schedule (
            term,
            class_code,
            course_code,
            course_name,
            teacher_no,
            teacher_name,
            week_start,
            week_end,
            weekday,
            period_start,
            period_end,
            location,
            created_by,
            created_at,
            updated_at
        ) VALUES (
            #{term},
            #{classCode},
            #{courseCode},
            #{courseName},
            #{teacherNo},
            #{teacherName},
            #{weekStart},
            #{weekEnd},
            #{weekday},
            #{periodStart},
            #{periodEnd},
            #{location},
            #{createdBy},
            #{createdAt},
            #{updatedAt}
        )
    </insert>
</mapper>
